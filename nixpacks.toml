[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma db push --accept-data-loss",
  "npm run build"
]

[start]
cmd = "node dist/src/server.js"
